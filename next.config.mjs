/** @type {import('next').NextConfig} */
const nextConfig = {};

export default nextConfig;
// next.config.js
module.exports = {
  experimental: {
    serverActions: false, // desactiva server actions
  },
};
